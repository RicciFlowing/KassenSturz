


<ul class="budget-list">
  {{#each model as |budget|}}
    <li class="budget-list-item"> {{#link-to 'budgets.budget' budget}}
      <p class="upper-right remaining">
        {{balance-display budget.remaining}}
      </p>
       <h3> {{budget.name}} </h3>
      <progress value={{budget.percentage}} max="100">{{budget.percentage}}</progress>

       {{/link-to}}
     </li>
  {{/each}}
</ul>
<div class="center">
  {{#link-to 'budgets.new'}}
   <span class="plus-sign budget-new-symbol"> + </span>
  {{/link-to}}

  {{#link-to 'budgets.distribute' class="distribute-link"}}
  {{inline-svg "balance.svg" class="icon icon-link icon-balance-scale"}}
  {{/link-to}}


</div>



{{outlet}}
